server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db_local
    username: root
    password: 12Rose#2323
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    root: DEBUG